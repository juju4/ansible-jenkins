---

jenkins_extra_pkg:
    - monit
    - crontabs
    - libxml2
    - libxslt-devel
    - libxml2-devel

jenkins_user_sshkeytypes:
    - rsa

ufw_rulename_ssh: SSH
nginx_jenkins_conf: "{{ nginx_sitedir }}/jenkins.conf"
